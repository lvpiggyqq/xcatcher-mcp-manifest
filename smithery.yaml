# Legacy repository metadata for Smithery-compatible importers.
# Current Smithery publication should use:
# smithery mcp publish "https://xcatcher.top/mcp/" -n lvpiggyqq/xcatcher
name: xcatcher-remote-mcp
description: Fetch recent public X (Twitter) posts by handle with structured JSON and optional accountless x402 v2 USDC pay-per-crawl on Base.
startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
    additionalProperties: false
  commandFunction: |
    () => ({
      url: "https://xcatcher.top/mcp/",
      headers: {
        "Accept": "application/json",
        "Content-Type": "application/json"
      }
    })
exampleConfig: {}
